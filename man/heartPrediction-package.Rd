% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R:package-doc.R
\docType{package}
\name{heartPrediction-package}
\alias{heartPrediction}
\alias{heartPrediction-package}
\title{heartpredict: A reproducible heart disease prediction package}
\description{
\strong{Overview}
\itemize{
\item This package ships a \emph{pre-trained} model bundle for heart disease prediction.
\item It provides deployment-safe utilities for feature engineering, input coercion,
prediction (class + probability), evaluation, and interpretability plots.
\item The coursework-required interface is a single exported function that accepts a
\code{data.frame} of predictors and returns predicted \emph{class labels}
(not probabilities): see \code{\link{heart_predict}}.
}
}
\details{
\strong{What is inside the bundle?}
The model bundle (an \code{.rds} file in \code{inst/extdata/}) typically includes:
\itemize{
\item Trained models: logistic regression (\code{glmnet}), full random forest, and TopK random forest
\item Stored preprocessing objects required for consistent inference (e.g., a prepped recipe for baking)
\item A TopK feature list used by the compact deployed model
\item Default decision thresholds chosen from training out-of-fold predictions
\item Factor levels and required column names for robust input validation
}

\strong{Core workflow}
\enumerate{
\item Load the shipped bundle using \code{\link{heart_load_bundle}}.
\item Prepare new patient records as a \code{data.frame} with required feature columns.
\item Run \code{\link{heart_predict}} to obtain class labels (coursework requirement),
or \code{\link{heart_predict_proba}} for probabilities.
}

\strong{Main exported functions}
\itemize{
\item \code{\link{heart_predict}}: Predict class labels from new data (recommended default: TopK model).
\item \code{\link{heart_predict_proba}}: Predict positive-class probabilities in \eqn{[0, 1]}.
\item \code{\link{heart_load_bundle}}: Load the bundled \code{.rds} object (or a user-supplied path).
\item \code{\link{heart_add_features}} / \code{\link{heart_coerce_inputs}}: Deployment-safe preprocessing.
\item \code{\link{heart_eval_threshold}} / \code{\link{heart_calibration}}: Metrics and calibration diagnostics.
\item \code{\link{heart_importance_rf}} / \code{\link{heart_importance_logistic}} /
\code{\link{heart_plot_importance}}: Interpretability helpers.
}

\strong{Important notes}
\itemize{
\item The shipped models are intended for educational demonstration and reproducible deployment only.
\item Predictions are not medical advice and must not be used for clinical decision making.
\item For the TopK model, preprocessing must match training. Therefore the bundle stores
the necessary preprocessing object(s) so \code{recipes::bake()} behaves consistently.
}

\strong{Quick example}
\preformatted{
library(heartpredict)

b <- heart_load_bundle()

new_pat <- data.frame(
  Age = 54, Sex = "M", ChestPainType = "ATA", RestingBP = 140, Cholesterol = 289,
  FastingBS = "0", RestingECG = "Normal", MaxHR = 172, ExerciseAngina = "N",
  Oldpeak = 0.0, ST_Slope = "Up"
)

# Coursework-required output: class label
heart_predict(new_pat, bundle = b, model = "topk")

# Optional: probability output
heart_predict_proba(new_pat, bundle = b, model = "topk")
}
}
\seealso{
Useful links:
\itemize{
  \item \url{https://github.com/tian233-bot/heartPredictR}
  \item Report bugs at \url{https://github.com/tian233-bot/heartPredictR/issues}
}

}
\author{
\strong{Maintainer}: Haotian Guo \email{Haotian.Guo23@student.xjtlu.edu.cn}

}
\keyword{internal}
